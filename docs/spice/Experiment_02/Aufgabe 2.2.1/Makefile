DVIPS = dvips -Ppdf -G0 -t letter
XPDF  = xpdf -remote -raise
ACROREAD = acroread
XDVI	= xdvi
TR = | tr '=' '\134'

.SUFFIXES: .org 

clean:
	rm -rf *.raw *.log .cadence
	rm -rf *.eldo.*
	rm -rf *.eldo_*
	rm -rf *.txt *.net *.data

